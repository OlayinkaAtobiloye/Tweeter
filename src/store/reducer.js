const initialState = {
auth: false
}

const Reducer = (state = initialState, action) => {
    switch(action.type){


        default:
            break;
    }
    return state;
}

export default Reducer;